application.title=TARUMT Resorts System
application.vendor=<Your Name>
build.classes.dir=${build.dir}/classes
build.dir=build
build.generated.sources.dir=${build.dir}/generated-sources
build.test.classes.dir=${build.dir}/test/classes
dist.dir=dist
dist.jar=${dist.dir}/G9_DSA_Assignment.jar
javac.classpath=
javac.compilerargs=
javac.deprecation=false
javac.source=1.8
javac.target=1.8
javac.test.classpath=${javac.classpath}:${build.classes.dir}
main.class=tarumtresorts.client.TarumtResortsApplication
manifest.file=manifest.mf
platform.active=default_platform
run.classpath=${javac.classpath}:${build.classes.dir}
src.dir=src
test.src.dir=test
